class FormBuilder{constructor(fields){this.fields=fields;}render(id){let f=document.getElementById(id);this.fields.forEach(x=>{f.innerHTML+=`<label>${x.label}</label><input type="${x.type}" id="${x.label}"><br>`;});f.innerHTML+=`<button onclick="getFormData()">Submit</button>`;}getFormData(){let o={};this.fields.forEach(x=>{o[x.label]=document.getElementById(x.label).value;});console.log(o);return o;}}